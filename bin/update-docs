#!/bin/bash

set -ex

[[ ! "$(git status --porcelain 2>/dev/null)" = *\?\?* ]]

git checkout gh-pages
git merge master
docco -l linear code/**/*.rb
git add .
git commit -m "Updating docs."
git push origin HEAD
git checkout master
